name: CI/CD-Dev-Staging

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Echo message
        run: echo "commit sur dev et push sur la branch ${{ github.ref_name }}"
